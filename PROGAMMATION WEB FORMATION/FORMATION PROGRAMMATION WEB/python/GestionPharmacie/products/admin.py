from django.contrib import admin

# Register your models here.
#
# Django fournit une interface d'administration prete a l'emploi
# (visible sur /admin/, voir GestionPharmacie/urls.py) qui permet de
# creer/modifier/supprimer des donnees sans ecrire de vues ni de
# templates. Pour qu'un modele y apparaisse, il faut l'enregistrer ici,
# par exemple une fois qu'un modele Produit existera dans models.py :
#   from .models import Produit
#   admin.site.register(Produit)
#
# Ce fichier est vide car products/models.py ne definit encore aucun
# modele : il n'y a donc rien a administrer pour le moment.
